
import { GraphQLError } from "graphql";
// import StatusCodes from "../../../common/statusCodes.json";

const getTests = async () => {

    // try {

    //     const Users = context.prisma.getTests();

    //     if (!Users.length) {
    //         throw new GraphQLError('there are no users.', { extensions: { code: 'BAD_USER_INPUT' } });
    //     }

    //     return {
    //         data: Users,
    //         status: StatusCodes[200].code,
    //         msg: "SUCCESS"
    //     }


    // } catch (error) {
    //     return {
    //         data: null,
    //         status: StatusCodes[400].code,
    //         msg: "Failed"
    //     }
    // }

}

export default getTests

